# @caphtech/casegraph-importer-markdown

Built-in markdown importer plugin for CaseGraph.

This package exposes the `casegraph-importer-markdown` executable used by the CaseGraph CLI plugin protocol.
